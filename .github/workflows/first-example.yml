name: First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome step 1
              run: echo "Welcome step 1"
            
            - name: Step 2
              run: ls 
            
            - name: Read file
              run: cat README.md